<li class="{name}{@currentPath/}">
	<a class="nav" href="{root}{path}">{title}</a>
	{?.sub}
		<ul>
		{#.sub}
			{>"partials/navigation-item" page=page root=root /}
		{/.sub}
		</ul>
	{/.sub}
			
</li>
